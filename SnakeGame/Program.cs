namespace SnakeGame;
class Program
{
    static int mapWidth = 17;
    static int mapHeight = 15;

    static void Main()
    {
        Console.Clear();
        ConsoleIO.PrintMapEdge(mapWidth, mapHeight);
    }
}

